import FirestoreCollections from "../adapters/FirestoreCollections"
import { Reagent } from "../models/Reagents"

export class ReagentService {
  async getAllReagents(): Promise<Reagent[]> {
     return []
  }
}